<persistence-unit name="cars" transaction-type="RESOURCE_LOCAL">

    <!-- REGISTER ALL ENTITY CLASSES -->
    <class>assets.Car</class>
    <class>assets.User</class>
    <class>assets.Booking</class>
    <!-- add all entities you have -->

    <properties>

        <property name="javax.persistence.jdbc.driver"
                  value="org.postgresql.Driver"/>

        <property name="javax.persistence.jdbc.url"
                  value="jdbc:${DATABASE_URL}"/>

        <property name="hibernate.dialect"
                  value="org.hibernate.dialect.PostgreSQLDialect"/>

        <!-- TEMPORARY: force table creation -->
        <property name="hibernate.hbm2ddl.auto"
                  value="create"/>

        <property name="hibernate.show_sql" value="true"/>
        <property name="hibernate.format_sql" value="true"/>

    </properties>

</persistence-unit>
